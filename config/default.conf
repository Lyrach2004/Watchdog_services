# Fichier de configuration par défaut pour Watchdog Services

# Adresse email de l'administrateur
ADMIN_EMAIL="salmajabah339@gmail.com"

# Services à surveiller (séparés par des espaces, utilisez le nom exact du service Windows)
SERVICES="ssh mysql nginx"

# Intervalle de vérification en secondes
CHECK_INTERVAL=60

# Niveau de détail des logs (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL="INFO"
REPERTOIRE_LOGS="../logs"
